<ul>
    <li>
	  <%= link_to poll.name, poll_path(poll) %><br />
      <%= link_to poll.description, poll_path(poll) %>
      <%= button_to "Take Poll", question_path(question), :method => :get, :class => "button_to" %>
	</li>
</ul>
